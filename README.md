# OpenInvite Cards

## New card script

Use `newcard.sh` to copy `base.html` into a new folder under `cards/`.

```bash
./newcard.sh christmas
```

This creates `cards/christmas/index.html` and exits with an error if the target already exists.
