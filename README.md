# OpenInvite Cards

## New card script

Use `newcard.sh` to copy `base.html` into the `cards/` directory with a new name.

```bash
./newcard.sh christmas
```

This creates `cards/christmas.html` and exits with an error if the target already exists.
